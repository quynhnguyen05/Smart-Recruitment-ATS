#Quỳnh
| **ID** | **Task** | **Input/context** | **AI output** | **Human verification/decision** |
|---|---|---|---|---|
| **A-01** | **Soạn Project Charter (Output #1)** | Bối cảnh đề tài ATS, 5 role, workflow bắt buộc Job → Apply → Screen → Interview → Score → Offer/Reject, business problem gốc từ đề bài giảng viên. | AI viết Charter đầy đủ nhưng tự thêm vào mục Success Metrics ý "hệ thống tự động xếp hạng và đề xuất ứng viên phù hợp nhất" — tính năng không có trong đề bài gốc và vi phạm nguyên tắc AI không tự quyết định của môn học. | Đối chiếu với bảng đề tài gốc và nguyên tắc AI discipline đã thống nhất, phát hiện tính năng vượt scope, yêu cầu AI bỏ ý đó và thay bằng chỉ số đo được: "0 trường hợp AI tự đổi trạng thái Application". |
| **A-02** | **Viết PRD từ `requirements.md` đã confirm** | Toàn bộ 13 FR + 5 NFR + 3 BR đã chốt ở Bài 1. | PRD do AI viết có thêm Goal mới "G5: Tăng tỷ lệ ứng viên quay lại ứng tuyển lần sau nhờ trải nghiệm cá nhân hóa bằng AI" — mục tiêu này không xuất phát từ bất kỳ REQ nào đã duyệt. | Đối chiếu từng Goal trong PRD với `requirements.md` gốc, phát hiện G5 không có REQ nguồn, xóa bỏ khỏi PRD để giữ PRD nhất quán với requirement đã confirm. |
| **A-03** | **Story slicing cho 13 User Story** | PRD đã duyệt + khung 5 Epic/13 Story đã thống nhất trong nhóm. | AI đề xuất gộp US-ATS-09 (Interviewer điền Scorecard) và US-ATS-10 (Hiring Manager xem tổng hợp Scorecard) thành một story duy nhất "Quản lý đánh giá phỏng vấn", ước lượng 5 điểm. | Nhận thấy đề xuất vi phạm Definition of Ready vì story phải ≤3 điểm và đang chứa hai flow độc lập của hai role khác nhau. Yêu cầu AI tách lại thành 2 story độc lập như thiết kế gốc. |

#Quỳnh Như
| **ID** | **Task** | **Input/context** | **AI output** | **Human verification/decision** |
|---|---|---|---|---|
| **A-01** | **Tạo Prototype cho 4 flow rủi ro cao (Output #10)** | Prototype goal kiểm chứng 4 flow: CV Screening, Human Confirmation, Question Suggestion và AI Service Fallback. Persona chính là Recruiter bận rộn. Required states gồm idle, loading, processing, empty, network-error, confirmation, fallback, success. | AI đề xuất prototype cho flow Screening nhưng ban đầu cho phép Recruiter bấm Pass/Reject và đổi trạng thái Application ngay lập tức, đồng thời khi AI gặp lỗi thì khóa các nút thao tác. | **Kiểm tra theo Prototype Brief:** Đối chiếu lại nguyên tắc Human Confirmation và AI Fallback. Bắt AI sửa flow để Pass/Reject bắt buộc đi qua Confirmation Modal + checkbox xác nhận; khi network-error thì vẫn cho phép Recruiter thao tác thủ công, không disable các nút. |
| **A-02** | **Thiết kế Figma / Design System Handoff (Output #16)** | Design tokens, Component Inventory và UX Copy đã xác định cho Screening flow, gồm DecisionButton, MatchScoreBadge, ConfirmationModal, AISummaryCard và Input Fields. | AI tạo component specification nhưng ban đầu thiếu đầy đủ state của AISummaryCard và MatchScoreBadge, đồng thời chưa thể hiện rõ loading/fallback/error behavior trong component. | **Đối chiếu Design System:** Kiểm tra lại với Output #16 và Prototype Brief. Yêu cầu bổ sung đúng các state: AISummaryCard gồm loading/success/fallback; DecisionButton gồm default/hover/disabled/loading; MatchScoreBadge gồm high/medium/low; đồng thời cập nhật UX Copy cho AI Loading, Network Error và Empty Questions. |
| **A-03** | **Tổng hợp Usability Findings vòng 2 (sau khi sửa prototype)** | Prototype đã sửa theo 4 flow của Output #10, ghi chú quan sát thô từ 3 người test mới. | Khi được yêu cầu tổng hợp finding từ ghi chú quan sát, AI tự suy diễn thêm một "insight" rằng "người dùng không thích màu đỏ của nút Reject", dù không có participant nào nói điều này trong ghi chú gốc. | **Kiểm tra bằng chứng:** Đối chiếu toàn bộ ghi chú quan sát gốc, không tìm thấy bằng chứng cho insight này. Loại bỏ khỏi `usability-test.md` để tránh đưa suy diễn của AI vào finding chính thức — chỉ giữ lại finding có bằng chứng quan sát trực tiếp. |



### Phụ trách: Quỳnh Như - Role: UX/UI Designer

| **ID** | **Task** | **Input/context** | **AI output** | **Human verification/decision** |
| :--- | :--- | :--- | :--- | :--- |
| **A-UI-01** | **Tạo Prototype cho 4 flow rủi ro cao (Output #10)** | Prototype goal kiểm chứng 4 flow: CV Screening, Human Confirmation, Question Suggestion và AI Service Fallback. Persona chính là Recruiter bận rộn. Required states gồm idle, loading, processing, empty, network-error, confirmation, fallback, success. | AI đề xuất prototype cho flow Screening nhưng ban đầu cho phép Recruiter bấm Pass/Reject và đổi trạng thái Application ngay lập tức, đồng thời khi AI gặp lỗi thì khóa các nút thao tác. | **Kiểm tra theo Prototype Brief:** Đối chiếu lại nguyên tắc Human Confirmation và AI Fallback. Bắt AI sửa flow để Pass/Reject bắt buộc đi qua Confirmation Modal + checkbox xác nhận; khi network-error thì vẫn cho phép Recruiter thao tác thủ công, không disable các nút. |
| **A-UI-02** | **Thiết kế Figma / Design System Handoff (Output #16)** | Design tokens, Component Inventory và UX Copy đã xác định cho Screening flow, gồm DecisionButton, MatchScoreBadge, ConfirmationModal, AISummaryCard và Input Fields. | AI tạo component specification nhưng ban đầu thiếu đầy đủ state của AISummaryCard và MatchScoreBadge, đồng thời chưa thể hiện rõ loading/fallback/error behavior trong component. | **Đối chiếu Design System:** Kiểm tra lại với Output #16 và Prototype Brief. Yêu cầu bổ sung đúng các state: AISummaryCard gồm loading/success/fallback; DecisionButton gồm default/hover/disabled/loading; MatchScoreBadge gồm high/medium/low; đồng thời cập nhật UX Copy cho AI Loading, Network Error và Empty Questions. |
| **A-UI-03** | **Tổng hợp Usability Findings vòng 2 (sau khi sửa prototype)** | Prototype đã sửa theo 4 flow của Output #10, ghi chú quan sát thô từ 3 người test mới. | Khi được yêu cầu tổng hợp finding từ ghi chú quan sát, AI tự suy diễn thêm một "insight" rằng "người dùng không thích màu đỏ của nút Reject", dù không có participant nào nói điều này trong ghi chú gốc. | **Kiểm tra bằng chứng:** Đối chiếu toàn bộ ghi chú quan sát gốc, không tìm thấy bằng chứng cho insight này. Loại bỏ khỏi `usability-test.md` để tránh đưa suy diễn của AI vào finding chính thức — chỉ giữ lại finding có bằng chứng quan sát trực tiếp. |
| **A-UI-04** | **Thiết kế Auto Layout cho Flow C - Scorecard (US-ATS-09)** | Giao diện tĩnh Split-view của Flow C, yêu cầu thiết lập Auto Layout để màn hình co giãn responsive. | AI hướng dẫn kỹ thuật bọc Auto Layout "Bottom-up" nhưng đề xuất giữ nguyên thanh Sidebar mở rộng như các luồng trước đó để "đồng nhất". | **Đánh giá lại UX:** Nhận thấy Form Scorecard cần nhiều chiều ngang, nếu giữ Sidebar to sẽ làm hẹp trình xem CV. **Quyết định bác bỏ AI**, tự thiết lập Sidebar sang trạng thái Thu gọn (Collapsed) để tối ưu không gian làm việc cho Interviewer. |


#Tâm Như
| **ID** | **Task** | **Input/context** | **AI output** | **Human verification/decision** |
|---|---|---|---|---|
| **A-01** | **Glossary Draft** | Đề bài + Domain Knowledge | Sinh các thuật ngữ liên quan đến ATS và AI tuyển dụng. | Nhóm rà soát, loại bỏ các thuật ngữ trùng và chỉnh định nghĩa phù hợp với workflow. |
| **A-02** | **Thiết lập Source Priority** | Yêu cầu AI tổng hợp các quy tắc ưu tiên tài liệu. | AI đề xuất: "Khi có xung đột, AI sẽ tự động chọn tài liệu mới nhất làm chuẩn". | **Bắt lỗi logic:** Nhóm bác bỏ đề xuất này vì AI không được tự quyết định nghiệp vụ. Sửa lại nguyên tắc thành: "Không tự suy diễn, phải ưu tiên theo thứ tự, nếu không rõ phải trả lời KHÔNG ĐỦ DỮ LIỆU". |
| **A-03** | **Tạo câu hỏi Benchmark** | Yêu cầu AI tạo 20 câu hỏi test Vault bao phủ đủ Fact, Rule, Edge-case và Unknown. | AI tạo ra 20 câu, nhưng các câu Unknown AI lại tự suy luận để trả lời (VD: "Mật khẩu admin cần 8 ký tự" dù Vault không hề ghi). | **Ép AI tuân thủ luật:** Đã nhắc lại luật Grounding. Ép AI phải trả lời chính xác chữ "KHÔNG ĐỦ DỮ LIỆU" cho các câu Q14, Q15, Q16, Q17, Q18 và Q20. |
| **A-04** | **Test Q&A Benchmark** | Ném toàn bộ thư mục Vault vào AI, yêu cầu trả lời Q12 (Gửi Offer ngay mà không cần phỏng vấn được không?). | Ban đầu AI trả lời: "Được, nếu Match Score quá cao thì có thể linh động". | **Phát hiện AI ảo giác (Hallucination):** Bác bỏ câu trả lời. Yêu cầu AI phải đọc lại file 04-decision-log.md và BR-ATS-03. Lần thử sau AI đã trả lời đúng (Pass) là "Không được". |

#Tuyết Sương
| ID | Task | Input/context | AI output | Human verification/decision |
| :--- | :--- | :--- | :--- | :--- |
| A-01 | Tạo kịch bản Usability Test (Output #11) | Prototype goal, 4 core tasks và danh sách Personas tham gia test. | AI sinh ra kịch bản test và kết quả nhưng gộp chung vào một bảng duy nhất, không khớp với cấu trúc 2 bảng của giáo trình. | Kiểm chứng format: Đối chiếu với Case mẫu mục 18.11, phát hiện AI làm sai quy định. Yêu cầu tách thành 2 bảng riêng biệt (Bảng 1: Script, Bảng 2: Findings) để đúng chuẩn nộp bài. |
| A-02 | Phân tích rủi ro UX (Output #11) | Tình huống Recruiter duyệt CV dựa trên AI Match Score. | AI phân tích: Recruiter dễ tin mù quáng vào điểm số cao và bỏ qua lý do, đề xuất "Giấu nút Pass/Reject xuống dưới". | Xác nhận tính logic: Phân tích hành vi người dùng thực tế thấy hợp lý. Chấp nhận finding này và đưa vào quyết định sửa Prototype (buộc hiển thị Matched/Missing Skills kế bên Score). |
| A-03 | Lập danh sách User Stories (Output #13) | Danh sách 13 REQ-ATS của BA và chuẩn Agile. | AI tự ý thêm cột "Nguồn Requirement" (thành 6 cột) và viết cột Title quá dài dòng. | Bắt lỗi sai chuẩn: Kiểm tra lại giáo trình, xác nhận mẫu chỉ có 5 cột. Ép AI gọt lại bảng đúng 5 cột và sửa Title thành dạng Action ngắn gọn (<80 ký tự). |
| A-04 | Rà soát độ bao phủ (Coverage) của Stories (Output #13) | Yêu cầu phân rã toàn bộ tính năng của dự án Smart Recruitment. | AI sinh ra danh sách 12 User Stories nhưng bỏ sót hoàn toàn Epic/Story cho Role Admin. | Phát hiện thiếu Scope: Đối chiếu lại với danh sách REQ (có REQ-ATS-13). Bắt AI bổ sung thêm US-ATS-13 (Manage users, roles, permissions) để chốt danh sách 13 Stories. |
| A-05 | Chẻ Task cho Taiga Backlog (Output #15) | Yêu cầu hướng dẫn làm Output 15 cho 13 User Stories. | AI nhầm lẫn Output 15 là viết Acceptance Criteria (BDD - Given/When/Then) nên sinh ra mẫu Story Spec sai yêu cầu. | Chấn chỉnh AI: Phát hiện sai lệch nội dung, cung cấp ảnh mẫu chuẩn của giáo trình (Gồm Task, Owner, Estimate). Ép AI viết lại bảng chia Task Breakdown chi tiết để đưa lên hệ thống Taiga. |
| A-06 | Viết Test Strategy & Test Cases (Output #26) | Yêu cầu viết chiến lược kiểm thử cho luồng AI tính Match Score và toàn hệ thống. | AI sinh ra chiến lược kiểm thử dạng văn bản chung chung, thiếu cấu trúc phân tầng (Layers) và danh sách Test Case mẫu. | Ép format kỹ thuật: Cung cấp template chuẩn. Bắt AI map đúng định dạng này với context của dự án (kiểm thử RBAC, Explicit Confirmation, AI Fallback) để ra bảng Test Strategy cuối cùng. |

#Thảo
| ID | Task | Input/context | AI output | Human verification/decision |
| :--- | :--- | :--- | :--- | :--- |
| **A-01** | **Thiết kế Architecture** | Yêu cầu nghiệp vụ (FR/BR) và luồng xử lý của hệ thống ATS. | Đề xuất kiến trúc cho phép LLM gọi và lưu trực tiếp trạng thái Application vào Database để tối ưu tốc độ. | **Bắt lỗi bảo mật & Logic:** Bác bỏ đề xuất. Bắt buộc AI không được truy cập DB trực tiếp mà phải qua AI Orchestrator. Thêm quy tắc "append-only" cho dữ liệu AI sinh ra. |
| **A-02** | **Xây dựng Data Model** | Architecture, Glossary và các BR/NFR. | Vẽ Data Model tạo một bảng `Position` độc lập cho Job Title và ghi đè (overwrite) dữ liệu bảng `MatchResult` mỗi lần AI chạy lại. | **Ép tuân thủ rule:** Nhắc AI xóa bảng `Position` và gộp vào `JobPosting` theo đúng Decision Log. Yêu cầu `MatchResult` và `AuditLog` phải là dạng *append-only*. |
| **A-03** | **Viết API Contract** | Data Model (Output #18) và phân quyền (RBAC). | Liệt kê API hợp lý nhưng cấp quyền PUBLIC hoặc CANDIDATE cho endpoint `/api/applications/:id/ai-score`. | **Phát hiện rò rỉ dữ liệu:** Yêu cầu sửa đổi phân quyền (Auth). Endpoint tính điểm AI (ai-score) chỉ cấp cho RECRUITER và ADMIN. Ứng viên (CANDIDATE) chỉ được xem trạng thái status. |
